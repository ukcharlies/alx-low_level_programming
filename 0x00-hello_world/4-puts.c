#include <stdio.h>								
/**
 *main-Serves as the entry piont of a program
 *initialize the strings 
 *write the strings out
 *return-if no error return zero, otherrwise return non zero value
 */
int main(void)
{
char word[]="\"Programming is like building a multilingual puzzle";
puts(word);
returns (0);
}
