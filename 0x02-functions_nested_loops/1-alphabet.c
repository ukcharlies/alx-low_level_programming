#include "main.h"

/**
 *main - check code
 *print_alphabet - prints the english alphabet from a-z.
 *Return: Always 0.
*/
void print_alphabet(void)
{
int ch;

for (ch = 'a'; ch <= 'z'; ++ch)
_putchar(ch);
_putchar('\n');
}
