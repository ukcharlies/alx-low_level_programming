#include <stdio.h>
/**
* main - Prints the alphabet except the letters q and e.
*
* Return: 0 on success
*/
int main(void)
{
char r = 'a';
while (r <= 'z')
{
if (r != 'q' && c != 'e')
{
putchar(r);
}
r++;
}
putchar('\n');
return (0);
}
